# Day3 实现记录（审美资产化深化 + Demo1 一周年重构）

日期：2026-03-09  
产品名称：InviteVerse  
阶段结论：Day3 将“审美主张”进一步拆解为可执行页面资产，并完成了 Demo1 一周年重构 + Demo2 赛博朋克深化版 + Demo3 产品发布现场。

## 1. Day2 基线回顾（已完成与边界）

1. Day2 主链路可运行：Creator -> Spec -> Guest。
2. Day2 已有 Spec 驱动渲染、回执工具、地图与分享能力。
3. Day2 对后端链路仍为预留：前端会尝试调用 `/api/spec/generate`，当前真实后端尚未接入。
4. Day3 的实施约束：所有功能代码仍在 `web/day2` 目录持续迭代，不改动 Day1 冻结快照。

## 2. Day3 产品策略（为什么这次要强调“审美资产化”）

1. InviteVerse 的差异化不只是“能生成邀请函”，而是“能持续生成有审美主张、可复用、可扩展的邀请体验”。
2. 审美资产化目标：把主理人的设计经验写成机器可调用规则，而不是每次从零拼页面。
3. 规则拆层：
   1. Theme DNA：场景、关系强度、情绪密度、私密感。
   2. Template Rules：布局、字体、色板、玻璃质感、动效曲线。
   3. Runtime Spec：最终渲染字段（event/copy/tokens/actions）。
4. 业务价值：
   1. 同一主题可批量复用。
   2. 不同主题可稳定扩展。
   3. LLM 输出受规则约束，降低“风格漂移”。

## 3. Demo1 升级目标（本次重构范围）

1. 主题从“情侣三周年纪念”改为“情侣一周年纪念”。
2. 叙事从一句话升级为完整四幕故事：
   1. 首屏信封开场（心动重申）。
   2. 解锁具体赴约信息（时间地点与二人备注）。
   3. 校园故事正文（相识、相恋、宿命高光）。
   4. 结尾确认赴约（情绪闭环）。
3. 内容素材紧扣设定：情侣相识于中国科大大一阶段，保留校园记忆与命运感。
4. 视觉与动效目标：
   1. 保留温暖浪漫。
   2. 提升层次与质感。
   3. 减少“空页感”，增强一体化叙事体验。

## 4. Demo1 详细实现拆解（重点）

### 4.1 主题语义定义

1. 关键词：温馨、私密、命定、校园回忆、郑重邀请。
2. 情绪节奏：
   1. 首屏克制（安静进入）。
   2. 中段揭示（信息解锁）。
   3. 故事升温（高光时刻串联）。
   4. 结尾收束（确认赴约）。

### 4.2 视觉系统（Color / Type / Material）

1. 色彩：以奶油粉、暖杏、香槟金为主，避免高对比硬边，保持晚霞与烛光气质。
2. 字体：`Cormorant Garamond + Noto Serif SC`，兼顾优雅拉丁字形与中文正文可读性。
3. 材质：玻璃拟态卡片（毛玻璃 + 轻边框 + 内高光）用于承载“私密信封”语义。
4. 背景层次：
   1. 缓慢漂移的暖色渐变底。
   2. 全屏模糊层。
   3. 细颗粒纹理层。
   4. 指针暖光层（跟随鼠标/触控移动）。

### 4.3 动效系统（Motion Choreography）

1. 首屏卡片入场：
   1. `y: 20 -> 0`。
   2. `opacity: 0 -> 1`。
   3. 时长 `1.5s`。
   4. 曲线 `cubic-bezier(0.25, 1, 0.5, 1)`。
2. 卡片呼吸：首屏入场后叠加轻微呼吸，维持生命感但不过度戏剧化。
3. 解锁反馈：滚动触发 `is-unlocked`，详情卡遮罩上移形成“揭封”体验。
4. 页面推进：每个内容区块 `IntersectionObserver` 分段显现，避免一次性堆砌信息。
5. 氛围微动：漂浮花瓣 + 指针光晕 + 向上提示箭头，形成持续但克制的动态环境。

### 4.4 内容系统（故事与信息设计）

1. 首屏文案：明确“一周年”与回到科大场景，建立时间和地点锚点。
2. 赴约信息卡：时间、地点、二人备注三项完整，强化“只属于两个人”的私密感。
3. 校园故事正文：
   1. 起点：大一新生周在东区三教相识。
   2. 发展：图书馆闭馆前互改作业、操场流星愿望、实验室凌晨同行。
   3. 定义：通过“我们不是偶然”强化宿命感。
4. 高光时刻模块：三条时间轴卡片（2024·09 相识 / 2025·03·15 确定关系 / 2026·03 一周年）让故事具象可感。
5. 结尾 CTA：情绪金句 + 日期确认 + “确认赴约”按钮，完成体验闭环。

### 4.5 交互与可用性

1. 移动端优先适配：卡片圆角、字号、行高、分栏在小屏下降级为单列。
2. 降噪策略：采用单一暖色系和少量交互点，避免“炫技型噪音”。
3. 可访问性：
   1. 保留语义化 section/aria-label。
   2. 提供 `prefers-reduced-motion` 降级。
   3. 交互反馈通过 toast 告知状态。

## 5. 本次代码改动（Day2 目录）

1. `web/day2/demo1_anniversary.html`：重构为一周年完整叙事页面。
2. `web/day2/demo1.css`：重写视觉系统、动效系统、响应式与降级策略。
3. `web/day2/demo1.js`：新增分段 reveal、解锁逻辑、指针光晕跟随、CTA toast。
4. `web/day2/creator.html`：入口文案改为“预览 Demo1（一周年）”。

## 6. `/api/spec/generate` 当前状态与 Day3 后续任务

当前状态：
1. 前端调用路径仍存在。
2. 真实后端仍未接入。

后续任务（按优先级）：
1. 接入后端 `/api/spec/generate`，让内容与风格字段由模型 + 规则协同生成。
2. 建立 Spec Schema 双层校验（后端校验 + 前端 normalize）。
3. 把 Demo1 中验证有效的视觉字段沉淀为模板资产（如 `anniversary_ustc_v1`）。
4. 把 Demo2 与 Demo3 进一步参数化为可复用模板，形成可对比的发布类模板矩阵。

## 7. Demo2 目标定义（赛博朋克生日派对）

设计目标：
1. 先产出“中文版”，提升阅读效率：除主标题 `CYBER BIRTHDAY PARTY` 保留英文外，其他标题与正文统一中文表达。
2. 内容从“短宣言”升级到“任务简报”：身份设定、主题动机、任务详情、交互按钮一体化。
3. 视觉严格贴合 Cyberpunk 2077 调色板：赛博黄 `#FCEE0A`、霓虹青 `#00F0FF`、故障红 `#FF003C`、深渊黑 `#0B0B0C`。
4. 交互上落地进场序列、glitch 主标题、毫秒级时钟、机械按压反馈与背景光雨。

技术落地策略：
1. 先按“单文件 React”方案实现，便于复制演示。
2. 线上验证时发现部分环境会出现 ESM 外链加载失败，导致正文不渲染（仅剩背景层）。
3. 为保证可用性，最终改为“本地静态可运行版”（HTML + CSS + JS），彻底去除运行时外链依赖。
4. 通过 CSS 伪元素 + `clip-path` + 微随机位移实现主标题 glitch；通过 JS 实现序列显现与按压反馈。

## 8. Demo2 详细实现拆解（已完成）

### 8.1 背景系统（黑底、网格、CRT）

1. 黑底：页面基础背景固定为纯黑。
2. 网格：双层 `linear-gradient` 生成赛博网格，透明度控制在 0.32，避免喧宾夺主。
3. CRT 扫描线：`repeating-linear-gradient` 构造横向扫描线，并加入缓慢漂移。
4. 工业噪声：叠加轻量 noise 纹理层，营造旧屏幕信号失真感。
5. 暗角：vignette 层压暗边缘，聚焦中区信息。

### 8.2 排版系统（Brutalism + 中文可读）

1. 主标题：`CYBER BIRTHDAY PARTY`（英文保留，作为视觉锚点）。
2. 其余文案：统一中文任务简报风格，降低理解门槛。
3. 字体策略：全局 `font-mono`，字重偏粗，工业感优先。
4. 版式策略：状态栏 -> 英文大标题 -> 中文协议覆盖行 -> Mission Intel 卡 -> CTA。

### 8.3 核心 Glitch 视效（严格执行）

1. 主标题使用 `::before`、`::after` 两个伪元素复制文本。
2. 两层分别使用青色/品红色，形成 RGB 通道撕裂感。
3. 通过 `clip-path: inset(...)` 关键帧切片，制造撕裂扫描错位。
4. JS 每 120ms 随机更新 `--glitch-x / --glitch-y`，叠加高频随机位移。
5. 随机短暂亮度脉冲（flicker）强化“故障瞬断”。

### 8.4 按钮交互（机械感）

1. CTA 使用矩形硬边按钮，不使用圆角胶囊语义。
2. 主按钮初始态：赛博黄底黑字；hover 变霓虹青并伴随瞬时故障抖动。
3. 次按钮语义明确：拒绝协议（故障红边框）。
4. 点击反馈：`pointerdown` 添加 `is-pressed`，`scale(0.95)` 下压感明显。
5. 点击后 toast 反馈：分别对应“接入网络/拒绝协议”。

### 8.5 内容结构（赛博派对语境）

1. 顶部状态栏：`[SYS_UPDATE] CYBER BIRTHDAY PARTY`。
2. 协议覆盖行：`>_ 协议覆盖：[Lucas] 迎来了他的 20 岁生日。`。
3. 主理人设定（最终文案）：`我是Lucas, 5 月 31 日是我 20 岁生日。想邀请我的朋友们一起登上外滩游艇, 把这一晚过成真正的赛博生日局!`。
4. Mission Intel 卡片字段（完整落地）：
   1. `目标 TARGET`：20 岁生日派对。
   2. `时间 TIME_SYNC`：`2026.05.31 // 19:00:00` + 毫秒级跳动时钟。
   3. `坐标 COORD`：`上海外滩码头 登陆游艇`。
   4. `装备要求 DRESS_CODE`：`赛博穿搭，怎么炫酷怎么来`。
5. 精简策略（最终确认）：
   1. 已按产品决策删除 `YACHT ZONES`、`NIGHT SCRIPT`、`CREW PROTOCOL` 三个模块。
   2. 页面信息密度聚焦在“核心邀约信息 + 交互按钮”，降低阅读负担。
6. CTA 区：
   1. 主按钮：`[ 接入网络 // JACK IN ]`。
   2. 次按钮：`[ 拒绝协议 // DECLINE ]`。

### 8.6 文件与代码落点

1. `web/day2/demo2_cyberpunk.html`：
   1. 使用本地可运行的静态结构（不依赖 React 外链模块）。
   2. 完成中文化文案与 Mission Briefing 架构。
   3. 修复“只显示网格、正文不显示”的兼容性问题。
   4. 完成最终文案定稿与三模块精简。
2. `web/day2/demo2.css`：
   1. 补充次按钮样式（DECLINE）与 CTA 区域双按钮布局。
   2. 保持赛博网格、扫描线、glitch、工业边框语言一致。
   3. 保留样式向后兼容，支持后续再次扩展内容区。
3. `web/day2/demo2.js`：
   1. 新增毫秒级 `TIME_SYNC` 实时跳动。
   2. 保留倒计时与高频 glitch。
   3. 增加主/次按钮按压与 toast 反馈。
4. `web/day2/creator.html`：
   1. 入口文案更新为：`预览 Demo2（赛博朋克）`。

### 8.7 自 Review（本次提交前检查）

1. 主题一致性：
   1. 全页围绕“赛博朋克 2077 风格 20 岁游艇生日局”。
   2. 配色、排版、组件形态、动效均在同一视觉语义内。
2. 时间与身份一致性：
   1. 生日时间固定：`2026-05-31 19:00`。
   2. 主办人文案固定为 Lucas 一人称邀请语气，符合真实场景。
   3. 坐标与穿搭字段均已替换为最终中文版本。
3. 交互有效性：
   1. 序列显现、glitch、扫描线、光雨、按钮机械反馈与时钟跳动均可运行。
   2. `prefers-reduced-motion` 降级已保留。
4. 移动端可用性：
   1. 主内容与按钮在小屏不溢出。
   2. 核心信息区保持可读。

### 8.8 预览路径

1. 直接打开：`/web/day2/demo2_cyberpunk.html`
2. 或从 Creator 页面点击：`预览 Demo2（赛博朋克）`

---

本记录目标：让团队成员在 10 分钟内看清 Day3 的设计决策、实现边界、代码落点和下一步执行路径。


## 9. Demo2 双语切换增量（2026-03-09）

### 9.1 本次目标

1. Creator 页面入口文案从 `预览 Demo2（赛博朋克-中文版）` 统一为 `预览 Demo2（赛博朋克）`。
2. Demo2 页面改为中英双语切换：中文与英文仅文案不同，布局、结构、色彩、动效保持一致。
3. 保持赛博朋克视觉统一性：切角、网格、扫描线、glitch、机械按压反馈不降级。

### 9.2 代码实现

1. `web/day2/creator.html`
   1. Demo2 入口文案调整为：`预览 Demo2（赛博朋克）`。
2. `web/day2/demo2_cyberpunk.html`
   1. 在顶部状态栏新增语言切换按钮 `#lang-toggle`。
   2. 为所有可切换文案增加 `data-i18n` 锚点（标题、副标题、信息卡、按钮、底部提示）。
3. `web/day2/demo2.js`
   1. 新增 `localeText` 双语字典（`zh` / `en`）。
   2. 新增 `applyLanguage(lang)`，统一刷新页面文案与 `html lang`。
   3. 新增本地记忆：`localStorage` key 为 `inviteverse_demo2_lang`。
   4. 倒计时按语言输出：
      1. 中文：`x天 xx时 xx分`
      2. 英文：`xD xxH xxM`
   5. `TIME_SYNC` 与 Toast 文案跟随语言切换。
4. `web/day2/demo2.css`
   1. 新增 `header-row` 与 `lang-toggle` 赛博风格样式。
   2. 主次按钮统一切角（Clipped Corners）表达。
   3. 主按钮改为赛博黄基调，hover 切换霓虹青；次按钮使用故障红语义。
   4. 移动端补充 `lang-toggle` 的字号与内边距适配，避免拥挤。

### 9.3 自检与 Review

1. 功能一致性
   1. 默认中文显示，点击切换按钮可在中英文之间来回切换。
   2. 切换后仅文案变化，结构和动效不变。
2. 视觉一致性
   1. 主标题 `CYBER BIRTHDAY PARTY` 在中英版本保持一致。
   2. 网格、扫描线、glitch、光效、按钮反馈保持同一设计语言。
3. 兼容性
   1. 继续使用静态 HTML/CSS/JS 实现，不依赖运行时 ESM 外链，避免“只显示网格”的历史问题。
   2. 保留 `prefers-reduced-motion` 降级规则。

### 9.4 建议提交信息（不执行提交）

`feat(day2-demo2): add zh/en language toggle for cyberpunk invite and unify creator entry label`

## 10. Demo3：InviteVerse 产品发布现场（硬核、极客、未来科技）

### 10.1 本次目标

1. 在 `web/day2` 内补齐 Demo3，作为 InviteVerse 自身产品发布的终极邀请函场景。
2. 设计语言明确收束到终端命令行、单色黑客绿/冰川蓝、打字机输出、数据网格与授权解锁。
3. 保证 `python -m http.server` 环境下可直接运行，不依赖 React 或外部 ESM 运行时。

### 10.2 设计策略

1. 视觉方向不走 Demo1 的情绪叙事，也不走 Demo2 的霓虹冲撞，而是强调“系统启动”“资产编译”“权限授权”的极客语义。
2. 页面被拆为三个连续层次：
   1. 背景层：Canvas 绘制缓慢旋转的 3D 线框点阵，表达“把审美沉淀为资产”的逻辑感。
   2. 主叙事层：终端面板逐字打印初始化日志、产品宣言与路演邀请。
   3. 解锁层：用户必须输入 `JOIN` 并回车，才能触发授权白闪并显示邀请码二维矩阵。
3. 整体交互节奏是“冷启动 -> 打印完成 -> 输入命令 -> 成功授权 -> 露出二维码”，让路演邀请更像一次系统接入，而不是普通海报页。

### 10.3 文案结构

最终主文案流程落地为：
1. `> Initializing InviteVerse Engine... [OK]`
2. `> Loading Aesthetic Assets... [OK]`
3. `> Compiling Interaction Grammar... [OK]`
4. `> 邀请函生成完毕。`
5. `『为你生命中的每个重要时刻，生成声画并茂、可交互的动态邀请函。』`
6. `2026年3月15日，InviteVerse 产品首发路演，准时接入。`
7. `> Venue: Hackathon Final Demo Stage // Judges Ready`
8. `> Command Required: JOIN`

### 10.4 代码落点

1. `web/day2/demo3_launch.html`
   1. 新增 Demo3 页面骨架。
   2. 包含状态栏、终端主体、命令输入区、二维码结果区四个核心部分。
2. `web/day2/demo3.css`
   1. 新增深色终端视觉系统、扫描线、网格、玻璃化面板与授权白闪动画。
   2. 统一使用等宽字体栈，保证极客语义与中英文可读性。
3. `web/day2/demo3.js`
   1. 新增终端打字机逻辑，逐行打印日志与邀请文案。
   2. 新增 Canvas 线框点阵背景动画。
   3. 新增 `JOIN` 命令校验、错误反馈、授权成功白闪与二维码矩阵生成逻辑。
4. `web/day2/creator.html`
   1. 新增 Creator 入口：`预览 Demo3（产品发布）`。

### 10.5 核心交互说明

1. 页面初始不会一次性展示所有文本，终端日志必须逐字出现。
2. 在日志打印完成前，命令输入框处于禁用状态，避免交互节奏被打断。
3. 用户输入错误命令时，会在终端中追加错误日志，并触发命令框抖动反馈。
4. 用户输入 `JOIN` 后，页面会闪一次授权白光，并显示邀请码二维矩阵与路演接入码。
5. 二维矩阵采用本地算法生成的 QR 风格矩阵，不依赖第三方二维码库，优先保证视觉完整性与离线可运行性。

### 10.6 自 Review

1. 主题一致性：背景、文案、输入方式、成功反馈都围绕“系统授权接入”展开，没有混入卡片式营销组件。
2. 视觉克制性：只使用黑、冰川蓝、黑客绿三类主色，没有引入多余高饱和撞色，和 Demo1、Demo2 明确区分。
3. 可运行性：延续 Demo2 的经验，采用本地 HTML/CSS/JS 静态实现，避免模块加载失败导致正文不显示。
4. 可演示性：终端打字、JOIN 解锁、白闪授权、二维码露出，这四个动作足够支撑黑客松现场展示。

### 10.7 预览路径

1. 直接打开：`/web/day2/demo3_launch.html`
2. 或从 Creator 页面点击：`预览 Demo3（产品发布）`


### 10.8 Demo3 细化优化（2026-03-10）

1. 字体策略优化：
   1. 英文与代码继续保留系统等宽栈 `ui-monospace / SFMono-Regular / Menlo / Consolas / Liberation Mono`，维持终端与代码感。
   2. 中文不再直接落到浏览器默认 CJK fallback，而是单独切换到 `Sarasa Mono SC / PingFang SC / Hiragino Sans GB / Microsoft YaHei UI / Microsoft YaHei / Noto Sans SC` 这一组更适合屏幕阅读的中文字体栈。
   3. 具体落点为：终端中文日志行、命令提示文本、结果区中文标题与正文。
2. 授权白闪动效优化：
   1. 初版实现是单层径向白光瞬时淡出，信息表达够直接，但审美上偏“功能性提示”。
   2. 本次改为三层复合授权光效：
      1. 中心冷白光核。
      2. 横向扫描式冰蓝能量带。
      3. 外围低饱和蓝绿辉光。
   3. 动效曲线从短促闪断调整为“爆发 -> 扫过 -> 余辉消散”，同时在闪动期间轻微提亮终端和解锁面板，让授权成功更像一场系统接入，而不是普通白屏闪烁。
3. Review 结论：
   1. 英文字体保持原先优势，没有破坏代码气质。
   2. 中文阅读感明显改善，字形不再过度方正，和终端界面的冷静气质更统一。
   3. 白闪由单点刺激改为带空间层次的视觉事件，展示感更强，也更适合路演现场演示。
